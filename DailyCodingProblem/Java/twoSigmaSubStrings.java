class Solution
{
  public static void subStringFunction(String input)
  {
    HashSet<String> vowels = new HashMap<String>();
    vowels.add("a");
    vowels.add("e");
    vowels.add("i");
    vowels.add("o");
    vowels.add("u");

    // Dynamically determine all substrings given an input
    // Append only the ones with that start with a vowel and end with a consonstant
    // if(string[0] in vowels && (string[string.length - 1] not in vowels)):
    // append to final output

  }
}
